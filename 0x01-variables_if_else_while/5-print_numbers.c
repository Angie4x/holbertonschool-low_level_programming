#include <stdio.h>
/**
 * main - main function
 * Return: always 0.
*/
int main(void)
{
	char low;

	for (low = 'a'; low <= 'z'; low++)
	putchar(low);

	putchar ('\n');

	return (0);
}tdio.h>
 /**
  * main - main function
* Return: always 0.
*/
int main(void)
{
char low;
for (low = '0'; low <= '9'; low++)
putchar(low);
putchar ('\n');
return (0);
}~
